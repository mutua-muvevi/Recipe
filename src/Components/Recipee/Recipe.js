import React from "react";

const Recipe = (props) => {
    return (
        <div>
            <h1>The specific Recipe</h1>
            <p>This is where we output the single item(recipe) 
                ingredient and render them here
            </p>
        </div>
    )
}

export default Recipe